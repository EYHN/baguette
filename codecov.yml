coverage:
  status:
    project:
      default:
        target: auto
        threshold: 1%
    patch:
      default:
        target: 80%
        threshold: 1%

# Mirror the lcov exclusions in .github/workflows/ci.yml: IO / 3rd-party
# shims around CoreSimulator, SimulatorKit, IOKit HID, Hummingbird,
# VideoToolbox, CoreImage, IOSurface and stdin/stdout sinks need a booted
# simulator or live socket to exercise — and the thin command-layer files
# that do nothing but wire those shims together follow the same rule.
ignore:
  - "Sources/BaguetteCore/Resources/**"
  - "Sources/BaguetteCore/Infrastructure/Server/**"
  - "Sources/BaguetteCore/Infrastructure/Simulator/**"
  - "Sources/BaguetteCore/Infrastructure/Input/**"
  - "Sources/BaguetteCore/Infrastructure/Screen/**"
  - "Sources/BaguetteCore/Infrastructure/Stream/**"
  - "Sources/BaguetteCore/Infrastructure/Chrome/PDFRasterizer.swift"
  - "Sources/BaguetteCore/App/Commands/**"
  - "Sources/BaguetteCore/App/Logger.swift"
  - "Tests/**"
